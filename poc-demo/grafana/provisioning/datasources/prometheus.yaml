apiVersion: 1

datasources:
  - name: Prometheus
    uid: bnaas-prom
    type: prometheus
    access: proxy
    # container-internal port, so it does not move with PORT_OFFSET
    url: http://prometheus:9090
    isDefault: true
    jsonData:
      timeInterval: 5s
      httpMethod: POST
